kind = "web"
previewPath = "/"
title = "Spam Purge"
version = "1.0.0"
id = "artifacts/spam-purge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21246

[services.development]
run = "pnpm --filter @workspace/spam-purge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/spam-purge", "run", "build" ]
publicDir = "artifacts/spam-purge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21246"
BASE_PATH = "/"
